<h1 class="text-center">Delete <%= @group.name %></h1>
<%= form_tag({action: 'destroy', id: @group.id}) do %>
<p>Are you sure you want to delete this item?</p>
<div class="form-group">
	<%= button_tag "Delete", {class: "btn btn-danger"} %>
</div>
<% end %>